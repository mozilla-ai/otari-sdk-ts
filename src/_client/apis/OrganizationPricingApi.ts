/* tslint:disable */
/* eslint-disable */
/**
 * otari
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime.js';
import {
    type HTTPValidationError,
    HTTPValidationErrorFromJSON,
    HTTPValidationErrorToJSON,
} from '../models/HTTPValidationError.js';
import {
    type OrganizationModelPricingCreate,
    OrganizationModelPricingCreateFromJSON,
    OrganizationModelPricingCreateToJSON,
} from '../models/OrganizationModelPricingCreate.js';
import {
    type OrganizationModelPricingPublic,
    OrganizationModelPricingPublicFromJSON,
    OrganizationModelPricingPublicToJSON,
} from '../models/OrganizationModelPricingPublic.js';
import {
    type OrganizationModelPricingUpdate,
    OrganizationModelPricingUpdateFromJSON,
    OrganizationModelPricingUpdateToJSON,
} from '../models/OrganizationModelPricingUpdate.js';
import {
    type OrganizationModelPricingsPublic,
    OrganizationModelPricingsPublicFromJSON,
    OrganizationModelPricingsPublicToJSON,
} from '../models/OrganizationModelPricingsPublic.js';

export interface OrganizationPricingCreateOrganizationPricingRequest {
    organizationModelPricingCreate: OrganizationModelPricingCreate;
}

export interface OrganizationPricingDeleteOrganizationPricingRequest {
    pricingId: string;
}

export interface OrganizationPricingListOrganizationPricingRequest {
    skip?: number;
    limit?: number;
}

export interface OrganizationPricingReplaceOrganizationPricingRequest {
    pricingId: string;
    organizationModelPricingUpdate: OrganizationModelPricingUpdate;
}

/**
 * 
 */
export class OrganizationPricingApi extends runtime.BaseAPI {

    /**
     * Creates request options for organizationPricingCreateOrganizationPricing without sending the request
     */
    async organizationPricingCreateOrganizationPricingRequestOpts(requestParameters: OrganizationPricingCreateOrganizationPricingRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationModelPricingCreate'] == null) {
            throw new runtime.RequiredError(
                'organizationModelPricingCreate',
                'Required parameter "organizationModelPricingCreate" was null or undefined when calling organizationPricingCreateOrganizationPricing().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-api-key"] = await this.configuration.apiKey("x-api-key"); // XApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Otari-Key"] = await this.configuration.apiKey("Otari-Key"); // ApiKeyAuth authentication
        }


        let urlPath = `/api/v1/organizations/me/pricing`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: OrganizationModelPricingCreateToJSON(requestParameters['organizationModelPricingCreate']),
        };
    }

    /**
     * Set the organization\'s rate for a model over a period.  Refused with a 409 when the period overlaps one already stored for that model, naming the period it collides with, rather than shadowing it.  The key is normalized to its canonical ``instance:model`` form first, the same call ``POST /api/v1/pricing`` makes, and that is what makes one model one row rather than one per spelling. Stored verbatim, ``openai:gpt-4o`` and ``openai/gpt-4o`` are two keys: the overlap rule would not see them as colliding, and both would resolve, with the canonical one preferred, leaving the other dormant until the first is deleted. Normalizing on the way in is what stops that pair existing at all.
     * Create Organization Pricing
     */
    async organizationPricingCreateOrganizationPricingRaw(requestParameters: OrganizationPricingCreateOrganizationPricingRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OrganizationModelPricingPublic>> {
        const requestOptions = await this.organizationPricingCreateOrganizationPricingRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OrganizationModelPricingPublicFromJSON(jsonValue));
    }

    /**
     * Set the organization\'s rate for a model over a period.  Refused with a 409 when the period overlaps one already stored for that model, naming the period it collides with, rather than shadowing it.  The key is normalized to its canonical ``instance:model`` form first, the same call ``POST /api/v1/pricing`` makes, and that is what makes one model one row rather than one per spelling. Stored verbatim, ``openai:gpt-4o`` and ``openai/gpt-4o`` are two keys: the overlap rule would not see them as colliding, and both would resolve, with the canonical one preferred, leaving the other dormant until the first is deleted. Normalizing on the way in is what stops that pair existing at all.
     * Create Organization Pricing
     */
    async organizationPricingCreateOrganizationPricing(requestParameters: OrganizationPricingCreateOrganizationPricingRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OrganizationModelPricingPublic> {
        const response = await this.organizationPricingCreateOrganizationPricingRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for organizationPricingDeleteOrganizationPricing without sending the request
     */
    async organizationPricingDeleteOrganizationPricingRequestOpts(requestParameters: OrganizationPricingDeleteOrganizationPricingRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['pricingId'] == null) {
            throw new runtime.RequiredError(
                'pricingId',
                'Required parameter "pricingId" was null or undefined when calling organizationPricingDeleteOrganizationPricing().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-api-key"] = await this.configuration.apiKey("x-api-key"); // XApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Otari-Key"] = await this.configuration.apiKey("Otari-Key"); // ApiKeyAuth authentication
        }


        let urlPath = `/api/v1/organizations/me/pricing/{pricing_id}`;
        urlPath = urlPath.replace('{pricing_id}', encodeURIComponent(String(requestParameters['pricingId'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Remove an override, returning the model to the deployment price list.
     * Delete Organization Pricing
     */
    async organizationPricingDeleteOrganizationPricingRaw(requestParameters: OrganizationPricingDeleteOrganizationPricingRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.organizationPricingDeleteOrganizationPricingRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Remove an override, returning the model to the deployment price list.
     * Delete Organization Pricing
     */
    async organizationPricingDeleteOrganizationPricing(requestParameters: OrganizationPricingDeleteOrganizationPricingRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.organizationPricingDeleteOrganizationPricingRaw(requestParameters, initOverrides);
    }

    /**
     * Creates request options for organizationPricingListOrganizationPricing without sending the request
     */
    async organizationPricingListOrganizationPricingRequestOpts(requestParameters: OrganizationPricingListOrganizationPricingRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['skip'] != null) {
            queryParameters['skip'] = requestParameters['skip'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-api-key"] = await this.configuration.apiKey("x-api-key"); // XApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Otari-Key"] = await this.configuration.apiKey("Otari-Key"); // ApiKeyAuth authentication
        }


        let urlPath = `/api/v1/organizations/me/pricing`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * List the organization\'s rate overrides.  Readable by any member: these rates decide what the caller\'s own requests cost, so they are not withheld from the people billed at them. Writing needs an owner or admin.  Paged on the same bounds the rest of the tenancy surface uses, because the table grows a row per model per period. ``count`` is the total, so a client knows whether another page is owed.
     * List Organization Pricing
     */
    async organizationPricingListOrganizationPricingRaw(requestParameters: OrganizationPricingListOrganizationPricingRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OrganizationModelPricingsPublic>> {
        const requestOptions = await this.organizationPricingListOrganizationPricingRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OrganizationModelPricingsPublicFromJSON(jsonValue));
    }

    /**
     * List the organization\'s rate overrides.  Readable by any member: these rates decide what the caller\'s own requests cost, so they are not withheld from the people billed at them. Writing needs an owner or admin.  Paged on the same bounds the rest of the tenancy surface uses, because the table grows a row per model per period. ``count`` is the total, so a client knows whether another page is owed.
     * List Organization Pricing
     */
    async organizationPricingListOrganizationPricing(requestParameters: OrganizationPricingListOrganizationPricingRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OrganizationModelPricingsPublic> {
        const response = await this.organizationPricingListOrganizationPricingRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for organizationPricingReplaceOrganizationPricing without sending the request
     */
    async organizationPricingReplaceOrganizationPricingRequestOpts(requestParameters: OrganizationPricingReplaceOrganizationPricingRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['pricingId'] == null) {
            throw new runtime.RequiredError(
                'pricingId',
                'Required parameter "pricingId" was null or undefined when calling organizationPricingReplaceOrganizationPricing().'
            );
        }

        if (requestParameters['organizationModelPricingUpdate'] == null) {
            throw new runtime.RequiredError(
                'organizationModelPricingUpdate',
                'Required parameter "organizationModelPricingUpdate" was null or undefined when calling organizationPricingReplaceOrganizationPricing().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-api-key"] = await this.configuration.apiKey("x-api-key"); // XApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Otari-Key"] = await this.configuration.apiKey("Otari-Key"); // ApiKeyAuth authentication
        }


        let urlPath = `/api/v1/organizations/me/pricing/{pricing_id}`;
        urlPath = urlPath.replace('{pricing_id}', encodeURIComponent(String(requestParameters['pricingId'])));

        return {
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: OrganizationModelPricingUpdateToJSON(requestParameters['organizationModelPricingUpdate']),
        };
    }

    /**
     * Replace an override\'s rates and period.  Future requests in the period price at the new rate; usage already settled keeps the cost it was billed, because a settled cost is stored on the usage row rather than recomputed.
     * Replace Organization Pricing
     */
    async organizationPricingReplaceOrganizationPricingRaw(requestParameters: OrganizationPricingReplaceOrganizationPricingRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OrganizationModelPricingPublic>> {
        const requestOptions = await this.organizationPricingReplaceOrganizationPricingRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OrganizationModelPricingPublicFromJSON(jsonValue));
    }

    /**
     * Replace an override\'s rates and period.  Future requests in the period price at the new rate; usage already settled keeps the cost it was billed, because a settled cost is stored on the usage row rather than recomputed.
     * Replace Organization Pricing
     */
    async organizationPricingReplaceOrganizationPricing(requestParameters: OrganizationPricingReplaceOrganizationPricingRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OrganizationModelPricingPublic> {
        const response = await this.organizationPricingReplaceOrganizationPricingRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
