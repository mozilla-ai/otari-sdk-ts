/* tslint:disable */
/* eslint-disable */
/**
 * otari
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime';
import {
    type CreateSessionRequest,
    CreateSessionRequestFromJSON,
    CreateSessionRequestToJSON,
} from '../models/CreateSessionRequest';
import {
    type HTTPValidationError,
    HTTPValidationErrorFromJSON,
    HTTPValidationErrorToJSON,
} from '../models/HTTPValidationError';
import {
    type SessionResponse,
    SessionResponseFromJSON,
    SessionResponseToJSON,
} from '../models/SessionResponse';

export interface CreateSessionV1AuthSessionPostRequest {
    createSessionRequest: CreateSessionRequest;
}

/**
 * 
 */
export class AuthApi extends runtime.BaseAPI {

    /**
     * Creates request options for createSessionV1AuthSessionPost without sending the request
     */
    async createSessionV1AuthSessionPostRequestOpts(requestParameters: CreateSessionV1AuthSessionPostRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['createSessionRequest'] == null) {
            throw new runtime.RequiredError(
                'createSessionRequest',
                'Required parameter "createSessionRequest" was null or undefined when calling createSessionV1AuthSessionPost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';


        let urlPath = `/v1/auth/session`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateSessionRequestToJSON(requestParameters['createSessionRequest']),
        };
    }

    /**
     * Verify the master key and set the HttpOnly session cookie.  The rate-limit check deliberately runs only after a failed verification, not before it: a pre-verification gate can\'t know whether *this* attempt would have succeeded, so once an IP has used up its failure quota it would end up blocking that IP\'s legitimate owner too, not just further attackers. The issue this implements explicitly rules that out. The DB/hash lookup this exposes to repeated attempts only runs when no fixed master_key is configured (the auto-generated bootstrap-key path); with a configured master_key, verification is a constant-time string compare, not a DB round trip.
     * Create Session
     */
    async createSessionV1AuthSessionPostRaw(requestParameters: CreateSessionV1AuthSessionPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SessionResponse>> {
        const requestOptions = await this.createSessionV1AuthSessionPostRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SessionResponseFromJSON(jsonValue));
    }

    /**
     * Verify the master key and set the HttpOnly session cookie.  The rate-limit check deliberately runs only after a failed verification, not before it: a pre-verification gate can\'t know whether *this* attempt would have succeeded, so once an IP has used up its failure quota it would end up blocking that IP\'s legitimate owner too, not just further attackers. The issue this implements explicitly rules that out. The DB/hash lookup this exposes to repeated attempts only runs when no fixed master_key is configured (the auto-generated bootstrap-key path); with a configured master_key, verification is a constant-time string compare, not a DB round trip.
     * Create Session
     */
    async createSessionV1AuthSessionPost(requestParameters: CreateSessionV1AuthSessionPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SessionResponse> {
        const response = await this.createSessionV1AuthSessionPostRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for deleteSessionV1AuthSessionDelete without sending the request
     */
    async deleteSessionV1AuthSessionDeleteRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/v1/auth/session`;

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Sign out: revoke the cookie\'s session server-side and expire the cookie.  Deliberately unauthenticated and idempotent: it only ever revokes the session named by the caller\'s own cookie, and the dashboard calls it on the 401-bounce path where no valid credential exists anymore. Unlike the read path in ``deps.py`` it applies no Sec-Fetch-Site check: ``SameSite=Strict`` already keeps cross-site requests from carrying the cookie, and the worst a forged call could do is sign the operator out.
     * Delete Session
     */
    async deleteSessionV1AuthSessionDeleteRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.deleteSessionV1AuthSessionDeleteRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Sign out: revoke the cookie\'s session server-side and expire the cookie.  Deliberately unauthenticated and idempotent: it only ever revokes the session named by the caller\'s own cookie, and the dashboard calls it on the 401-bounce path where no valid credential exists anymore. Unlike the read path in ``deps.py`` it applies no Sec-Fetch-Site check: ``SameSite=Strict`` already keeps cross-site requests from carrying the cookie, and the worst a forged call could do is sign the operator out.
     * Delete Session
     */
    async deleteSessionV1AuthSessionDelete(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteSessionV1AuthSessionDeleteRaw(initOverrides);
    }

}
