/* tslint:disable */
/* eslint-disable */
/**
 * otari
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime.js';
import {
    type CreateSearchToolRequest,
    CreateSearchToolRequestFromJSON,
    CreateSearchToolRequestToJSON,
} from '../models/CreateSearchToolRequest.js';
import {
    type HTTPValidationError,
    HTTPValidationErrorFromJSON,
    HTTPValidationErrorToJSON,
} from '../models/HTTPValidationError.js';
import {
    type ReencryptSearchToolsResponse,
    ReencryptSearchToolsResponseFromJSON,
    ReencryptSearchToolsResponseToJSON,
} from '../models/ReencryptSearchToolsResponse.js';
import {
    type SearchProviderSchema,
    SearchProviderSchemaFromJSON,
    SearchProviderSchemaToJSON,
} from '../models/SearchProviderSchema.js';
import {
    type SearchToolsResponse,
    SearchToolsResponseFromJSON,
    SearchToolsResponseToJSON,
} from '../models/SearchToolsResponse.js';
import {
    type StoredSearchToolSchema,
    StoredSearchToolSchemaFromJSON,
    StoredSearchToolSchemaToJSON,
} from '../models/StoredSearchToolSchema.js';
import {
    type UpdateSearchToolRequest,
    UpdateSearchToolRequestFromJSON,
    UpdateSearchToolRequestToJSON,
} from '../models/UpdateSearchToolRequest.js';

export interface SearchToolsCreateSearchToolRequest {
    createSearchToolRequest: CreateSearchToolRequest;
}

export interface SearchToolsDeleteStoredSearchToolRequest {
    name: string;
}

export interface SearchToolsUpdateSearchToolRequest {
    name: string;
    updateSearchToolRequest: UpdateSearchToolRequest;
}

/**
 * 
 */
export class SearchToolsApi extends runtime.BaseAPI {

    /**
     * Creates request options for searchToolsCreateSearchTool without sending the request
     */
    async searchToolsCreateSearchToolRequestOpts(requestParameters: SearchToolsCreateSearchToolRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['createSearchToolRequest'] == null) {
            throw new runtime.RequiredError(
                'createSearchToolRequest',
                'Required parameter "createSearchToolRequest" was null or undefined when calling searchToolsCreateSearchTool().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-api-key"] = await this.configuration.apiKey("x-api-key"); // XApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Otari-Key"] = await this.configuration.apiKey("Otari-Key"); // ApiKeyAuth authentication
        }


        let urlPath = `/api/v1/search-tools`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateSearchToolRequestToJSON(requestParameters['createSearchToolRequest']),
        };
    }

    /**
     * Add a search tool at runtime. Storing an API key requires OTARI_SECRET_KEY.
     * Create Search Tool
     */
    async searchToolsCreateSearchToolRaw(requestParameters: SearchToolsCreateSearchToolRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<StoredSearchToolSchema>> {
        const requestOptions = await this.searchToolsCreateSearchToolRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => StoredSearchToolSchemaFromJSON(jsonValue));
    }

    /**
     * Add a search tool at runtime. Storing an API key requires OTARI_SECRET_KEY.
     * Create Search Tool
     */
    async searchToolsCreateSearchTool(requestParameters: SearchToolsCreateSearchToolRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<StoredSearchToolSchema> {
        const response = await this.searchToolsCreateSearchToolRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for searchToolsDeleteStoredSearchTool without sending the request
     */
    async searchToolsDeleteStoredSearchToolRequestOpts(requestParameters: SearchToolsDeleteStoredSearchToolRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling searchToolsDeleteStoredSearchTool().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-api-key"] = await this.configuration.apiKey("x-api-key"); // XApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Otari-Key"] = await this.configuration.apiKey("Otari-Key"); // ApiKeyAuth authentication
        }


        let urlPath = `/api/v1/search-tools/{name}`;
        urlPath = urlPath.replace('{name}', encodeURIComponent(String(requestParameters['name'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Delete a stored search tool. A config-file search tool cannot be deleted here.
     * Delete Stored Search Tool
     */
    async searchToolsDeleteStoredSearchToolRaw(requestParameters: SearchToolsDeleteStoredSearchToolRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.searchToolsDeleteStoredSearchToolRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Delete a stored search tool. A config-file search tool cannot be deleted here.
     * Delete Stored Search Tool
     */
    async searchToolsDeleteStoredSearchTool(requestParameters: SearchToolsDeleteStoredSearchToolRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.searchToolsDeleteStoredSearchToolRaw(requestParameters, initOverrides);
    }

    /**
     * Creates request options for searchToolsListAllSearchTools without sending the request
     */
    async searchToolsListAllSearchToolsRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-api-key"] = await this.configuration.apiKey("x-api-key"); // XApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Otari-Key"] = await this.configuration.apiKey("Otari-Key"); // ApiKeyAuth authentication
        }


        let urlPath = `/api/v1/search-tools`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * List every search tool ``POST /api/v1/search`` can name.  ``stored`` are the editable rows written through this API; ``config`` are the config-file entries, which are still honored and are reported so the operator can see the whole set. Keys are never returned, only ``last4``.
     * List All Search Tools
     */
    async searchToolsListAllSearchToolsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SearchToolsResponse>> {
        const requestOptions = await this.searchToolsListAllSearchToolsRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SearchToolsResponseFromJSON(jsonValue));
    }

    /**
     * List every search tool ``POST /api/v1/search`` can name.  ``stored`` are the editable rows written through this API; ``config`` are the config-file entries, which are still honored and are reported so the operator can see the whole set. Keys are never returned, only ``last4``.
     * List All Search Tools
     */
    async searchToolsListAllSearchTools(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SearchToolsResponse> {
        const response = await this.searchToolsListAllSearchToolsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for searchToolsListSearchProviders without sending the request
     */
    async searchToolsListSearchProvidersRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-api-key"] = await this.configuration.apiKey("x-api-key"); // XApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Otari-Key"] = await this.configuration.apiKey("Otari-Key"); // ApiKeyAuth authentication
        }


        let urlPath = `/api/v1/search-tools/providers`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * List the search providers this build can dispatch to, for the add-tool form.  Reports per provider whether an API key is required and what endpoint a tool inherits when it declares none, so the form can ask for exactly what the chosen provider needs instead of taking a free-text provider name.
     * List Search Providers
     */
    async searchToolsListSearchProvidersRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<SearchProviderSchema>>> {
        const requestOptions = await this.searchToolsListSearchProvidersRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(SearchProviderSchemaFromJSON));
    }

    /**
     * List the search providers this build can dispatch to, for the add-tool form.  Reports per provider whether an API key is required and what endpoint a tool inherits when it declares none, so the form can ask for exactly what the chosen provider needs instead of taking a free-text provider name.
     * List Search Providers
     */
    async searchToolsListSearchProviders(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<SearchProviderSchema>> {
        const response = await this.searchToolsListSearchProvidersRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for searchToolsReencryptStoredSearchToolKeys without sending the request
     */
    async searchToolsReencryptStoredSearchToolKeysRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-api-key"] = await this.configuration.apiKey("x-api-key"); // XApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Otari-Key"] = await this.configuration.apiKey("Otari-Key"); // ApiKeyAuth authentication
        }


        let urlPath = `/api/v1/search-tools/reencrypt`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Re-encrypt stored search-tool keys with the primary OTARI_SECRET_KEY.  The search-tool half of the ``OTARI_SECRET_KEY`` rotation procedure; run it alongside ``POST /api/v1/provider-credentials/reencrypt``. Rows that cannot be decrypted are left untouched and must be recovered by replacing the affected tool\'s key.
     * Reencrypt Stored Search Tool Keys
     */
    async searchToolsReencryptStoredSearchToolKeysRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ReencryptSearchToolsResponse>> {
        const requestOptions = await this.searchToolsReencryptStoredSearchToolKeysRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ReencryptSearchToolsResponseFromJSON(jsonValue));
    }

    /**
     * Re-encrypt stored search-tool keys with the primary OTARI_SECRET_KEY.  The search-tool half of the ``OTARI_SECRET_KEY`` rotation procedure; run it alongside ``POST /api/v1/provider-credentials/reencrypt``. Rows that cannot be decrypted are left untouched and must be recovered by replacing the affected tool\'s key.
     * Reencrypt Stored Search Tool Keys
     */
    async searchToolsReencryptStoredSearchToolKeys(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ReencryptSearchToolsResponse> {
        const response = await this.searchToolsReencryptStoredSearchToolKeysRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for searchToolsUpdateSearchTool without sending the request
     */
    async searchToolsUpdateSearchToolRequestOpts(requestParameters: SearchToolsUpdateSearchToolRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling searchToolsUpdateSearchTool().'
            );
        }

        if (requestParameters['updateSearchToolRequest'] == null) {
            throw new runtime.RequiredError(
                'updateSearchToolRequest',
                'Required parameter "updateSearchToolRequest" was null or undefined when calling searchToolsUpdateSearchTool().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-api-key"] = await this.configuration.apiKey("x-api-key"); // XApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Otari-Key"] = await this.configuration.apiKey("Otari-Key"); // ApiKeyAuth authentication
        }


        let urlPath = `/api/v1/search-tools/{name}`;
        urlPath = urlPath.replace('{name}', encodeURIComponent(String(requestParameters['name'])));

        return {
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: UpdateSearchToolRequestToJSON(requestParameters['updateSearchToolRequest']),
        };
    }

    /**
     * Update a stored search tool. Omitted fields are left as-is; an explicit ``null`` clears them.  ``api_key`` follows the same rule: omit it to keep the stored key, send a new one to rotate, or send ``null`` to clear it (a keyless SearXNG backend). The row is locked ``FOR UPDATE`` so the ``expected_updated_at`` check and the write it guards are atomic. The tool as it will be after the update is validated, so a change that would leave it unusable (clearing the key of a provider that needs one) is refused rather than stored.
     * Update Search Tool
     */
    async searchToolsUpdateSearchToolRaw(requestParameters: SearchToolsUpdateSearchToolRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<StoredSearchToolSchema>> {
        const requestOptions = await this.searchToolsUpdateSearchToolRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => StoredSearchToolSchemaFromJSON(jsonValue));
    }

    /**
     * Update a stored search tool. Omitted fields are left as-is; an explicit ``null`` clears them.  ``api_key`` follows the same rule: omit it to keep the stored key, send a new one to rotate, or send ``null`` to clear it (a keyless SearXNG backend). The row is locked ``FOR UPDATE`` so the ``expected_updated_at`` check and the write it guards are atomic. The tool as it will be after the update is validated, so a change that would leave it unusable (clearing the key of a provider that needs one) is refused rather than stored.
     * Update Search Tool
     */
    async searchToolsUpdateSearchTool(requestParameters: SearchToolsUpdateSearchToolRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<StoredSearchToolSchema> {
        const response = await this.searchToolsUpdateSearchToolRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
