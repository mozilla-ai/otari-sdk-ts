/* tslint:disable */
/* eslint-disable */
/**
 * otari
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.12.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime.js';
import {
    type HTTPValidationError,
    HTTPValidationErrorFromJSON,
    HTTPValidationErrorToJSON,
} from '../models/HTTPValidationError.js';
import {
    type Message,
    MessageFromJSON,
    MessageToJSON,
} from '../models/Message.js';
import {
    type OrganizationGuardrailDefinitionCreate,
    OrganizationGuardrailDefinitionCreateFromJSON,
    OrganizationGuardrailDefinitionCreateToJSON,
} from '../models/OrganizationGuardrailDefinitionCreate.js';
import {
    type OrganizationGuardrailDefinitionPublic,
    OrganizationGuardrailDefinitionPublicFromJSON,
    OrganizationGuardrailDefinitionPublicToJSON,
} from '../models/OrganizationGuardrailDefinitionPublic.js';
import {
    type OrganizationGuardrailDefinitionTest,
    OrganizationGuardrailDefinitionTestFromJSON,
    OrganizationGuardrailDefinitionTestToJSON,
} from '../models/OrganizationGuardrailDefinitionTest.js';
import {
    type OrganizationGuardrailDefinitionTestResult,
    OrganizationGuardrailDefinitionTestResultFromJSON,
    OrganizationGuardrailDefinitionTestResultToJSON,
} from '../models/OrganizationGuardrailDefinitionTestResult.js';
import {
    type OrganizationGuardrailDefinitionUpdate,
    OrganizationGuardrailDefinitionUpdateFromJSON,
    OrganizationGuardrailDefinitionUpdateToJSON,
} from '../models/OrganizationGuardrailDefinitionUpdate.js';
import {
    type OrganizationGuardrailDefinitionsPublic,
    OrganizationGuardrailDefinitionsPublicFromJSON,
    OrganizationGuardrailDefinitionsPublicToJSON,
} from '../models/OrganizationGuardrailDefinitionsPublic.js';

export interface OrganizationGuardrailDefinitionsCreateOrganizationGuardrailDefinitionRequest {
    organizationGuardrailDefinitionCreate: OrganizationGuardrailDefinitionCreate;
}

export interface OrganizationGuardrailDefinitionsDeleteOrganizationGuardrailDefinitionRequest {
    definitionId: string;
}

export interface OrganizationGuardrailDefinitionsListOrganizationGuardrailDefinitionsRequest {
    skip?: number;
    limit?: number;
}

export interface OrganizationGuardrailDefinitionsTestOrganizationGuardrailDefinitionRequest {
    definitionId: string;
    organizationGuardrailDefinitionTest: OrganizationGuardrailDefinitionTest;
}

export interface OrganizationGuardrailDefinitionsUpdateOrganizationGuardrailDefinitionRequest {
    definitionId: string;
    organizationGuardrailDefinitionUpdate: OrganizationGuardrailDefinitionUpdate;
}

/**
 * 
 */
export class OrganizationGuardrailDefinitionsApi extends runtime.BaseAPI {

    /**
     * Creates request options for organizationGuardrailDefinitionsCreateOrganizationGuardrailDefinition without sending the request
     */
    async organizationGuardrailDefinitionsCreateOrganizationGuardrailDefinitionRequestOpts(requestParameters: OrganizationGuardrailDefinitionsCreateOrganizationGuardrailDefinitionRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationGuardrailDefinitionCreate'] == null) {
            throw new runtime.RequiredError(
                'organizationGuardrailDefinitionCreate',
                'Required parameter "organizationGuardrailDefinitionCreate" was null or undefined when calling organizationGuardrailDefinitionsCreateOrganizationGuardrailDefinition().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-api-key"] = await this.configuration.apiKey("x-api-key"); // XApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Otari-Key"] = await this.configuration.apiKey("Otari-Key"); // ApiKeyAuth authentication
        }


        let urlPath = `/api/v1/organizations/me/guardrail-definitions`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: OrganizationGuardrailDefinitionCreateToJSON(requestParameters['organizationGuardrailDefinitionCreate']),
        };
    }

    /**
     * Define a guardrail this deployment will build and call itself. Organization owners and admins only.  ``guardrail_name`` must be one the built-in guardrail catalog lists (``GET /api/v1/tool-settings/guardrails/catalog``), and ``create_kwargs`` must satisfy that guardrail\'s constructor as the catalog describes it: no argument it does not declare, nothing it types as a live object rather than configuration, and every required argument no environment variable can supply. Arguments the catalog marks secret are encrypted at rest.  A definition on its own changes no request. Mandate it through ``/api/v1/organizations/me/guardrails`` for it to run.  The definition is saved first and built second, so a guardrail this deployment cannot construct is still stored and answers ``build_state: \"failed\"`` rather than refusing the write. Why it failed is not reported: a vendor library may put the arguments it was handed, which are your credentials, into its own error message. The reason is in the gateway\'s log.
     * Create Organization Guardrail Definition
     */
    async organizationGuardrailDefinitionsCreateOrganizationGuardrailDefinitionRaw(requestParameters: OrganizationGuardrailDefinitionsCreateOrganizationGuardrailDefinitionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OrganizationGuardrailDefinitionPublic>> {
        const requestOptions = await this.organizationGuardrailDefinitionsCreateOrganizationGuardrailDefinitionRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OrganizationGuardrailDefinitionPublicFromJSON(jsonValue));
    }

    /**
     * Define a guardrail this deployment will build and call itself. Organization owners and admins only.  ``guardrail_name`` must be one the built-in guardrail catalog lists (``GET /api/v1/tool-settings/guardrails/catalog``), and ``create_kwargs`` must satisfy that guardrail\'s constructor as the catalog describes it: no argument it does not declare, nothing it types as a live object rather than configuration, and every required argument no environment variable can supply. Arguments the catalog marks secret are encrypted at rest.  A definition on its own changes no request. Mandate it through ``/api/v1/organizations/me/guardrails`` for it to run.  The definition is saved first and built second, so a guardrail this deployment cannot construct is still stored and answers ``build_state: \"failed\"`` rather than refusing the write. Why it failed is not reported: a vendor library may put the arguments it was handed, which are your credentials, into its own error message. The reason is in the gateway\'s log.
     * Create Organization Guardrail Definition
     */
    async organizationGuardrailDefinitionsCreateOrganizationGuardrailDefinition(requestParameters: OrganizationGuardrailDefinitionsCreateOrganizationGuardrailDefinitionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OrganizationGuardrailDefinitionPublic> {
        const response = await this.organizationGuardrailDefinitionsCreateOrganizationGuardrailDefinitionRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for organizationGuardrailDefinitionsDeleteOrganizationGuardrailDefinition without sending the request
     */
    async organizationGuardrailDefinitionsDeleteOrganizationGuardrailDefinitionRequestOpts(requestParameters: OrganizationGuardrailDefinitionsDeleteOrganizationGuardrailDefinitionRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['definitionId'] == null) {
            throw new runtime.RequiredError(
                'definitionId',
                'Required parameter "definitionId" was null or undefined when calling organizationGuardrailDefinitionsDeleteOrganizationGuardrailDefinition().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-api-key"] = await this.configuration.apiKey("x-api-key"); // XApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Otari-Key"] = await this.configuration.apiKey("Otari-Key"); // ApiKeyAuth authentication
        }


        let urlPath = `/api/v1/organizations/me/guardrail-definitions/{definition_id}`;
        urlPath = urlPath.replace('{definition_id}', encodeURIComponent(String(requestParameters['definitionId'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Drop a definition and the credentials it holds.  Organization owners and admins only. Use ``enabled: false`` instead to stop the guardrail everywhere while keeping both.
     * Delete Organization Guardrail Definition
     */
    async organizationGuardrailDefinitionsDeleteOrganizationGuardrailDefinitionRaw(requestParameters: OrganizationGuardrailDefinitionsDeleteOrganizationGuardrailDefinitionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Message>> {
        const requestOptions = await this.organizationGuardrailDefinitionsDeleteOrganizationGuardrailDefinitionRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MessageFromJSON(jsonValue));
    }

    /**
     * Drop a definition and the credentials it holds.  Organization owners and admins only. Use ``enabled: false`` instead to stop the guardrail everywhere while keeping both.
     * Delete Organization Guardrail Definition
     */
    async organizationGuardrailDefinitionsDeleteOrganizationGuardrailDefinition(requestParameters: OrganizationGuardrailDefinitionsDeleteOrganizationGuardrailDefinitionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Message> {
        const response = await this.organizationGuardrailDefinitionsDeleteOrganizationGuardrailDefinitionRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for organizationGuardrailDefinitionsListOrganizationGuardrailDefinitions without sending the request
     */
    async organizationGuardrailDefinitionsListOrganizationGuardrailDefinitionsRequestOpts(requestParameters: OrganizationGuardrailDefinitionsListOrganizationGuardrailDefinitionsRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['skip'] != null) {
            queryParameters['skip'] = requestParameters['skip'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-api-key"] = await this.configuration.apiKey("x-api-key"); // XApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Otari-Key"] = await this.configuration.apiKey("Otari-Key"); // ApiKeyAuth authentication
        }


        let urlPath = `/api/v1/organizations/me/guardrail-definitions`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * List the guardrails the caller\'s organization has defined.  Organization owners and admins only. A stored vendor credential is never returned: each one comes back as ``***`` under its own name, which is what a form resubmits to keep it.  ``build_state`` answers for the worker that served this request, so a read taken moments after a write may still report ``pending`` on a sibling that has not caught up.
     * List Organization Guardrail Definitions
     */
    async organizationGuardrailDefinitionsListOrganizationGuardrailDefinitionsRaw(requestParameters: OrganizationGuardrailDefinitionsListOrganizationGuardrailDefinitionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OrganizationGuardrailDefinitionsPublic>> {
        const requestOptions = await this.organizationGuardrailDefinitionsListOrganizationGuardrailDefinitionsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OrganizationGuardrailDefinitionsPublicFromJSON(jsonValue));
    }

    /**
     * List the guardrails the caller\'s organization has defined.  Organization owners and admins only. A stored vendor credential is never returned: each one comes back as ``***`` under its own name, which is what a form resubmits to keep it.  ``build_state`` answers for the worker that served this request, so a read taken moments after a write may still report ``pending`` on a sibling that has not caught up.
     * List Organization Guardrail Definitions
     */
    async organizationGuardrailDefinitionsListOrganizationGuardrailDefinitions(requestParameters: OrganizationGuardrailDefinitionsListOrganizationGuardrailDefinitionsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OrganizationGuardrailDefinitionsPublic> {
        const response = await this.organizationGuardrailDefinitionsListOrganizationGuardrailDefinitionsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for organizationGuardrailDefinitionsTestOrganizationGuardrailDefinition without sending the request
     */
    async organizationGuardrailDefinitionsTestOrganizationGuardrailDefinitionRequestOpts(requestParameters: OrganizationGuardrailDefinitionsTestOrganizationGuardrailDefinitionRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['definitionId'] == null) {
            throw new runtime.RequiredError(
                'definitionId',
                'Required parameter "definitionId" was null or undefined when calling organizationGuardrailDefinitionsTestOrganizationGuardrailDefinition().'
            );
        }

        if (requestParameters['organizationGuardrailDefinitionTest'] == null) {
            throw new runtime.RequiredError(
                'organizationGuardrailDefinitionTest',
                'Required parameter "organizationGuardrailDefinitionTest" was null or undefined when calling organizationGuardrailDefinitionsTestOrganizationGuardrailDefinition().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-api-key"] = await this.configuration.apiKey("x-api-key"); // XApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Otari-Key"] = await this.configuration.apiKey("Otari-Key"); // ApiKeyAuth authentication
        }


        let urlPath = `/api/v1/organizations/me/guardrail-definitions/{definition_id}/test`;
        urlPath = urlPath.replace('{definition_id}', encodeURIComponent(String(requestParameters['definitionId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: OrganizationGuardrailDefinitionTestToJSON(requestParameters['organizationGuardrailDefinitionTest']),
        };
    }

    /**
     * Run a definition\'s guardrail over some text and return its verdict.  Organization owners and admins only. The guardrail is the one the worker that answered already holds built, so this tests what is running rather than building it again. Nothing is stored and no mandate is involved; ``validate_kwargs`` stands in for what a mandate would pass with each check.  A definition this worker does not hold built answers 409, and its ``build_state`` says why. A vendor call that fails answers 502, and the reason is in the gateway\'s log only: a vendor library may put the credentials it was handed into its own message.
     * Test Organization Guardrail Definition
     */
    async organizationGuardrailDefinitionsTestOrganizationGuardrailDefinitionRaw(requestParameters: OrganizationGuardrailDefinitionsTestOrganizationGuardrailDefinitionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OrganizationGuardrailDefinitionTestResult>> {
        const requestOptions = await this.organizationGuardrailDefinitionsTestOrganizationGuardrailDefinitionRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OrganizationGuardrailDefinitionTestResultFromJSON(jsonValue));
    }

    /**
     * Run a definition\'s guardrail over some text and return its verdict.  Organization owners and admins only. The guardrail is the one the worker that answered already holds built, so this tests what is running rather than building it again. Nothing is stored and no mandate is involved; ``validate_kwargs`` stands in for what a mandate would pass with each check.  A definition this worker does not hold built answers 409, and its ``build_state`` says why. A vendor call that fails answers 502, and the reason is in the gateway\'s log only: a vendor library may put the credentials it was handed into its own message.
     * Test Organization Guardrail Definition
     */
    async organizationGuardrailDefinitionsTestOrganizationGuardrailDefinition(requestParameters: OrganizationGuardrailDefinitionsTestOrganizationGuardrailDefinitionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OrganizationGuardrailDefinitionTestResult> {
        const response = await this.organizationGuardrailDefinitionsTestOrganizationGuardrailDefinitionRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for organizationGuardrailDefinitionsUpdateOrganizationGuardrailDefinition without sending the request
     */
    async organizationGuardrailDefinitionsUpdateOrganizationGuardrailDefinitionRequestOpts(requestParameters: OrganizationGuardrailDefinitionsUpdateOrganizationGuardrailDefinitionRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['definitionId'] == null) {
            throw new runtime.RequiredError(
                'definitionId',
                'Required parameter "definitionId" was null or undefined when calling organizationGuardrailDefinitionsUpdateOrganizationGuardrailDefinition().'
            );
        }

        if (requestParameters['organizationGuardrailDefinitionUpdate'] == null) {
            throw new runtime.RequiredError(
                'organizationGuardrailDefinitionUpdate',
                'Required parameter "organizationGuardrailDefinitionUpdate" was null or undefined when calling organizationGuardrailDefinitionsUpdateOrganizationGuardrailDefinition().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-api-key"] = await this.configuration.apiKey("x-api-key"); // XApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Otari-Key"] = await this.configuration.apiKey("Otari-Key"); // ApiKeyAuth authentication
        }


        let urlPath = `/api/v1/organizations/me/guardrail-definitions/{definition_id}`;
        urlPath = urlPath.replace('{definition_id}', encodeURIComponent(String(requestParameters['definitionId'])));

        return {
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: OrganizationGuardrailDefinitionUpdateToJSON(requestParameters['organizationGuardrailDefinitionUpdate']),
        };
    }

    /**
     * Change a definition\'s name, guardrail, build arguments, or enabled flag.  Organization owners and admins only. Omitted fields are left as they are. ``create_kwargs`` replaces the arguments whole when sent, an argument sent as ``***`` keeps the value stored under that name, and omitting the field entirely leaves the stored credentials untouched and unread.  The guardrail is rebuilt afterwards and the response reports the outcome in ``build_state``, so repairing a credential shows the definition running again in the same response, and ``enabled: false`` stops it here rather than on the next refresh.
     * Update Organization Guardrail Definition
     */
    async organizationGuardrailDefinitionsUpdateOrganizationGuardrailDefinitionRaw(requestParameters: OrganizationGuardrailDefinitionsUpdateOrganizationGuardrailDefinitionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OrganizationGuardrailDefinitionPublic>> {
        const requestOptions = await this.organizationGuardrailDefinitionsUpdateOrganizationGuardrailDefinitionRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OrganizationGuardrailDefinitionPublicFromJSON(jsonValue));
    }

    /**
     * Change a definition\'s name, guardrail, build arguments, or enabled flag.  Organization owners and admins only. Omitted fields are left as they are. ``create_kwargs`` replaces the arguments whole when sent, an argument sent as ``***`` keeps the value stored under that name, and omitting the field entirely leaves the stored credentials untouched and unread.  The guardrail is rebuilt afterwards and the response reports the outcome in ``build_state``, so repairing a credential shows the definition running again in the same response, and ``enabled: false`` stops it here rather than on the next refresh.
     * Update Organization Guardrail Definition
     */
    async organizationGuardrailDefinitionsUpdateOrganizationGuardrailDefinition(requestParameters: OrganizationGuardrailDefinitionsUpdateOrganizationGuardrailDefinitionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OrganizationGuardrailDefinitionPublic> {
        const response = await this.organizationGuardrailDefinitionsUpdateOrganizationGuardrailDefinitionRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
