/* tslint:disable */
/* eslint-disable */
/**
 * otari
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime.js';
import {
    type CallToolResult,
    CallToolResultFromJSON,
    CallToolResultToJSON,
} from '../models/CallToolResult.js';
import {
    type McpErrorBody,
    McpErrorBodyFromJSON,
    McpErrorBodyToJSON,
} from '../models/McpErrorBody.js';
import {
    type McpExecuteRequest,
    McpExecuteRequestFromJSON,
    McpExecuteRequestToJSON,
} from '../models/McpExecuteRequest.js';
import {
    type McpToolsResponse,
    McpToolsResponseFromJSON,
    McpToolsResponseToJSON,
} from '../models/McpToolsResponse.js';

export interface McpExecuteMcpToolRequest {
    mcpExecuteRequest: McpExecuteRequest;
}

export interface McpListMcpToolsRequest {
    mcpServerId: string;
}

/**
 * 
 */
export class McpApi extends runtime.BaseAPI {

    /**
     * Creates request options for mcpExecuteMcpTool without sending the request
     */
    async mcpExecuteMcpToolRequestOpts(requestParameters: McpExecuteMcpToolRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['mcpExecuteRequest'] == null) {
            throw new runtime.RequiredError(
                'mcpExecuteRequest',
                'Required parameter "mcpExecuteRequest" was null or undefined when calling mcpExecuteMcpTool().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-api-key"] = await this.configuration.apiKey("x-api-key"); // XApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Otari-Key"] = await this.configuration.apiKey("Otari-Key"); // ApiKeyAuth authentication
        }


        let urlPath = `/api/v1/mcp/execute`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: McpExecuteRequestToJSON(requestParameters['mcpExecuteRequest']),
        };
    }

    /**
     * Execute one caller-authorized tool call against a stored MCP server.  The calling application owns any user approval, argument editing, cancellation and action history; Otari executes exactly the tool name and arguments it is given, once, and returns the remote server\'s native result. A result with ``isError: true`` is a definitive outcome and comes back as an HTTP 200.  **This request must never be retried automatically.** ``client_execution_id`` is correlation, not idempotency: once the call has been dispatched Otari cannot know whether the tool ran, and an ``outcome_unknown`` response means exactly that. Proxies, service meshes and SDKs on this path have to disable retries for it, including on connection resets and 5xx responses.
     * Execute Mcp Tool
     */
    async mcpExecuteMcpToolRaw(requestParameters: McpExecuteMcpToolRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CallToolResult>> {
        const requestOptions = await this.mcpExecuteMcpToolRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CallToolResultFromJSON(jsonValue));
    }

    /**
     * Execute one caller-authorized tool call against a stored MCP server.  The calling application owns any user approval, argument editing, cancellation and action history; Otari executes exactly the tool name and arguments it is given, once, and returns the remote server\'s native result. A result with ``isError: true`` is a definitive outcome and comes back as an HTTP 200.  **This request must never be retried automatically.** ``client_execution_id`` is correlation, not idempotency: once the call has been dispatched Otari cannot know whether the tool ran, and an ``outcome_unknown`` response means exactly that. Proxies, service meshes and SDKs on this path have to disable retries for it, including on connection resets and 5xx responses.
     * Execute Mcp Tool
     */
    async mcpExecuteMcpTool(requestParameters: McpExecuteMcpToolRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CallToolResult> {
        const response = await this.mcpExecuteMcpToolRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for mcpListMcpTools without sending the request
     */
    async mcpListMcpToolsRequestOpts(requestParameters: McpListMcpToolsRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['mcpServerId'] == null) {
            throw new runtime.RequiredError(
                'mcpServerId',
                'Required parameter "mcpServerId" was null or undefined when calling mcpListMcpTools().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-api-key"] = await this.configuration.apiKey("x-api-key"); // XApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Otari-Key"] = await this.configuration.apiKey("Otari-Key"); // ApiKeyAuth authentication
        }


        let urlPath = `/api/v1/mcp/servers/{mcp_server_id}/tools`;
        urlPath = urlPath.replace('{mcp_server_id}', encodeURIComponent(String(requestParameters['mcpServerId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * List the tools a stored MCP server exposes to the authenticated workspace.  Call this once per server when preparing a model or workflow run, and reuse the answer for every tool from that server for the length of the run: there is no cross-run cache in this version, so a later run rediscovers and staleness stays bounded without any invalidation state to keep.  The response is the whole authorized catalog or an error. It is never partial, because a caller would read a short catalog as the complete input to its own authorization and risk policy. The single exception is ``warnings``, which names a tool whose descriptor Otari could not carry.  A tool the server removes after discovery may still be proposed from the run\'s snapshot; execution then returns the remote server\'s own typed error.
     * List Mcp Tools
     */
    async mcpListMcpToolsRaw(requestParameters: McpListMcpToolsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<McpToolsResponse>> {
        const requestOptions = await this.mcpListMcpToolsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => McpToolsResponseFromJSON(jsonValue));
    }

    /**
     * List the tools a stored MCP server exposes to the authenticated workspace.  Call this once per server when preparing a model or workflow run, and reuse the answer for every tool from that server for the length of the run: there is no cross-run cache in this version, so a later run rediscovers and staleness stays bounded without any invalidation state to keep.  The response is the whole authorized catalog or an error. It is never partial, because a caller would read a short catalog as the complete input to its own authorization and risk policy. The single exception is ``warnings``, which names a tool whose descriptor Otari could not carry.  A tool the server removes after discovery may still be proposed from the run\'s snapshot; execution then returns the remote server\'s own typed error.
     * List Mcp Tools
     */
    async mcpListMcpTools(requestParameters: McpListMcpToolsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<McpToolsResponse> {
        const response = await this.mcpListMcpToolsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
