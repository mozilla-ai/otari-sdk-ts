/* tslint:disable */
/* eslint-disable */
/**
 * otari
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime.js';
import {
    type ActivationApiKeyPublic,
    ActivationApiKeyPublicFromJSON,
    ActivationApiKeyPublicToJSON,
} from '../models/ActivationApiKeyPublic.js';
import {
    type HTTPValidationError,
    HTTPValidationErrorFromJSON,
    HTTPValidationErrorToJSON,
} from '../models/HTTPValidationError.js';
import {
    type Message,
    MessageFromJSON,
    MessageToJSON,
} from '../models/Message.js';
import {
    type WorkspaceActivationPublic,
    WorkspaceActivationPublicFromJSON,
    WorkspaceActivationPublicToJSON,
} from '../models/WorkspaceActivationPublic.js';

export interface WorkspaceActivationCreateWorkspaceActivationKeyRequest {
    workspaceId: string;
}

export interface WorkspaceActivationDismissWorkspaceActivationRequest {
    workspaceId: string;
}

export interface WorkspaceActivationGetWorkspaceActivationRequest {
    workspaceId: string;
}

/**
 * 
 */
export class WorkspaceActivationApi extends runtime.BaseAPI {

    /**
     * Creates request options for workspaceActivationCreateWorkspaceActivationKey without sending the request
     */
    async workspaceActivationCreateWorkspaceActivationKeyRequestOpts(requestParameters: WorkspaceActivationCreateWorkspaceActivationKeyRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['workspaceId'] == null) {
            throw new runtime.RequiredError(
                'workspaceId',
                'Required parameter "workspaceId" was null or undefined when calling workspaceActivationCreateWorkspaceActivationKey().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-api-key"] = await this.configuration.apiKey("x-api-key"); // XApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Otari-Key"] = await this.configuration.apiKey("Otari-Key"); // ApiKeyAuth authentication
        }


        let urlPath = `/api/v1/workspaces/{workspace_id}/activation/key`;
        urlPath = urlPath.replace('{workspace_id}', encodeURIComponent(String(requestParameters['workspaceId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Issue the workspace\'s setup API key, rotating the one the guide already issued.  Workspace owners and admins only, like every other action that changes a workspace. The plaintext is returned once and never stored, so a reopened guide rotates the same key row rather than collecting a second one, and answers 409 once the workspace has activated or the guide was dismissed.
     * Create Workspace Activation Key
     */
    async workspaceActivationCreateWorkspaceActivationKeyRaw(requestParameters: WorkspaceActivationCreateWorkspaceActivationKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ActivationApiKeyPublic>> {
        const requestOptions = await this.workspaceActivationCreateWorkspaceActivationKeyRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ActivationApiKeyPublicFromJSON(jsonValue));
    }

    /**
     * Issue the workspace\'s setup API key, rotating the one the guide already issued.  Workspace owners and admins only, like every other action that changes a workspace. The plaintext is returned once and never stored, so a reopened guide rotates the same key row rather than collecting a second one, and answers 409 once the workspace has activated or the guide was dismissed.
     * Create Workspace Activation Key
     */
    async workspaceActivationCreateWorkspaceActivationKey(requestParameters: WorkspaceActivationCreateWorkspaceActivationKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ActivationApiKeyPublic> {
        const response = await this.workspaceActivationCreateWorkspaceActivationKeyRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for workspaceActivationDismissWorkspaceActivation without sending the request
     */
    async workspaceActivationDismissWorkspaceActivationRequestOpts(requestParameters: WorkspaceActivationDismissWorkspaceActivationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['workspaceId'] == null) {
            throw new runtime.RequiredError(
                'workspaceId',
                'Required parameter "workspaceId" was null or undefined when calling workspaceActivationDismissWorkspaceActivation().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-api-key"] = await this.configuration.apiKey("x-api-key"); // XApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Otari-Key"] = await this.configuration.apiKey("Otari-Key"); // ApiKeyAuth authentication
        }


        let urlPath = `/api/v1/workspaces/{workspace_id}/activation/dismiss`;
        urlPath = urlPath.replace('{workspace_id}', encodeURIComponent(String(requestParameters['workspaceId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Retire the guide for this workspace. Permanent, and idempotent.  Workspace owners and admins only. It retires the card and nothing else: the key the guide issued keeps working, because the operator asked for it and may well have pasted it somewhere already. Revoking one is the Keys page\'s job.
     * Dismiss Workspace Activation
     */
    async workspaceActivationDismissWorkspaceActivationRaw(requestParameters: WorkspaceActivationDismissWorkspaceActivationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Message>> {
        const requestOptions = await this.workspaceActivationDismissWorkspaceActivationRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MessageFromJSON(jsonValue));
    }

    /**
     * Retire the guide for this workspace. Permanent, and idempotent.  Workspace owners and admins only. It retires the card and nothing else: the key the guide issued keeps working, because the operator asked for it and may well have pasted it somewhere already. Revoking one is the Keys page\'s job.
     * Dismiss Workspace Activation
     */
    async workspaceActivationDismissWorkspaceActivation(requestParameters: WorkspaceActivationDismissWorkspaceActivationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Message> {
        const response = await this.workspaceActivationDismissWorkspaceActivationRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for workspaceActivationGetWorkspaceActivation without sending the request
     */
    async workspaceActivationGetWorkspaceActivationRequestOpts(requestParameters: WorkspaceActivationGetWorkspaceActivationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['workspaceId'] == null) {
            throw new runtime.RequiredError(
                'workspaceId',
                'Required parameter "workspaceId" was null or undefined when calling workspaceActivationGetWorkspaceActivation().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-api-key"] = await this.configuration.apiKey("x-api-key"); // XApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Otari-Key"] = await this.configuration.apiKey("Otari-Key"); // ApiKeyAuth authentication
        }


        let urlPath = `/api/v1/workspaces/{workspace_id}/activation`;
        urlPath = urlPath.replace('{workspace_id}', encodeURIComponent(String(requestParameters['workspaceId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Where a workspace stands on its first successful request.  Readable by any member who can see the workspace. Whether the guide should actually be offered to this caller is ``experience_eligible``, which also answers false when the deployment has the flow turned off (``activation_guide``), so a dashboard left open stops offering it without needing to be reloaded.
     * Get Workspace Activation
     */
    async workspaceActivationGetWorkspaceActivationRaw(requestParameters: WorkspaceActivationGetWorkspaceActivationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<WorkspaceActivationPublic>> {
        const requestOptions = await this.workspaceActivationGetWorkspaceActivationRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => WorkspaceActivationPublicFromJSON(jsonValue));
    }

    /**
     * Where a workspace stands on its first successful request.  Readable by any member who can see the workspace. Whether the guide should actually be offered to this caller is ``experience_eligible``, which also answers false when the deployment has the flow turned off (``activation_guide``), so a dashboard left open stops offering it without needing to be reloaded.
     * Get Workspace Activation
     */
    async workspaceActivationGetWorkspaceActivation(requestParameters: WorkspaceActivationGetWorkspaceActivationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<WorkspaceActivationPublic> {
        const response = await this.workspaceActivationGetWorkspaceActivationRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
