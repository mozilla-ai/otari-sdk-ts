/* tslint:disable */
/* eslint-disable */
/**
 * otari
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime.js';
import {
    type CatalogModelDetail,
    CatalogModelDetailFromJSON,
    CatalogModelDetailToJSON,
} from '../models/CatalogModelDetail.js';
import {
    type CatalogResponse,
    CatalogResponseFromJSON,
    CatalogResponseToJSON,
} from '../models/CatalogResponse.js';
import {
    type HTTPValidationError,
    HTTPValidationErrorFromJSON,
    HTTPValidationErrorToJSON,
} from '../models/HTTPValidationError.js';
import {
    type SelectorIndexResponse,
    SelectorIndexResponseFromJSON,
    SelectorIndexResponseToJSON,
} from '../models/SelectorIndexResponse.js';

export interface CatalogGetCatalogModelRequest {
    modelId: string;
}

export interface CatalogListCatalogRequest {
    atContext?: number | null;
    search?: string | null;
    skip?: number;
    limit?: number;
}

/**
 * 
 */
export class CatalogApi extends runtime.BaseAPI {

    /**
     * Creates request options for catalogGetCatalogModel without sending the request
     */
    async catalogGetCatalogModelRequestOpts(requestParameters: CatalogGetCatalogModelRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['modelId'] == null) {
            throw new runtime.RequiredError(
                'modelId',
                'Required parameter "modelId" was null or undefined when calling catalogGetCatalogModel().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-api-key"] = await this.configuration.apiKey("x-api-key"); // XApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Otari-Key"] = await this.configuration.apiKey("Otari-Key"); // ApiKeyAuth authentication
        }


        let urlPath = `/api/v1/catalog/models/{model_id}`;
        urlPath = urlPath.replace('{model_id}', encodeURIComponent(String(requestParameters['modelId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * One model and every offering of it this caller may use.  The whole merged catalog is built and grouped to answer for one model. That is deliberate: the identity a model is found by is a property of the group, so narrowing the build to one model would need the grouping done first. The query count is constant; the cost is CPU per page view, growing with the size of the catalog rather than with the number of readers.  A model the caller may not see answers 404, the same as one that does not exist, so the route cannot be used to probe the catalog behind an allow-list. A signed-in caller\'s offerings also carry their organization\'s own usage of each over the last 30 days.
     * Get Catalog Model
     */
    async catalogGetCatalogModelRaw(requestParameters: CatalogGetCatalogModelRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CatalogModelDetail>> {
        const requestOptions = await this.catalogGetCatalogModelRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CatalogModelDetailFromJSON(jsonValue));
    }

    /**
     * One model and every offering of it this caller may use.  The whole merged catalog is built and grouped to answer for one model. That is deliberate: the identity a model is found by is a property of the group, so narrowing the build to one model would need the grouping done first. The query count is constant; the cost is CPU per page view, growing with the size of the catalog rather than with the number of readers.  A model the caller may not see answers 404, the same as one that does not exist, so the route cannot be used to probe the catalog behind an allow-list. A signed-in caller\'s offerings also carry their organization\'s own usage of each over the last 30 days.
     * Get Catalog Model
     */
    async catalogGetCatalogModel(requestParameters: CatalogGetCatalogModelRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CatalogModelDetail> {
        const response = await this.catalogGetCatalogModelRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for catalogListCatalog without sending the request
     */
    async catalogListCatalogRequestOpts(requestParameters: CatalogListCatalogRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['atContext'] != null) {
            queryParameters['at_context'] = requestParameters['atContext'];
        }

        if (requestParameters['search'] != null) {
            queryParameters['search'] = requestParameters['search'];
        }

        if (requestParameters['skip'] != null) {
            queryParameters['skip'] = requestParameters['skip'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-api-key"] = await this.configuration.apiKey("x-api-key"); // XApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Otari-Key"] = await this.configuration.apiKey("Otari-Key"); // ApiKeyAuth authentication
        }


        let urlPath = `/api/v1/catalog/models`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * The models this caller may use, one entry each however many providers serve it.  Prices are the caller\'s: an organization\'s override where one applies, else the deployment\'s row, else the genai-prices default. Aliases and routing policies are not models and are not listed; see Routing. A visitor, where the catalog is public, sees the configured instances at the deployment\'s rates and nothing that belongs to a tenant.
     * List Catalog
     */
    async catalogListCatalogRaw(requestParameters: CatalogListCatalogRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CatalogResponse>> {
        const requestOptions = await this.catalogListCatalogRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CatalogResponseFromJSON(jsonValue));
    }

    /**
     * The models this caller may use, one entry each however many providers serve it.  Prices are the caller\'s: an organization\'s override where one applies, else the deployment\'s row, else the genai-prices default. Aliases and routing policies are not models and are not listed; see Routing. A visitor, where the catalog is public, sees the configured instances at the deployment\'s rates and nothing that belongs to a tenant.
     * List Catalog
     */
    async catalogListCatalog(requestParameters: CatalogListCatalogRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CatalogResponse> {
        const response = await this.catalogListCatalogRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for catalogRefreshSelectorIndex without sending the request
     */
    async catalogRefreshSelectorIndexRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-api-key"] = await this.configuration.apiKey("x-api-key"); // XApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Otari-Key"] = await this.configuration.apiKey("Otari-Key"); // ApiKeyAuth authentication
        }


        let urlPath = `/api/v1/catalog/selectors/refresh`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Re-index the short spellings now, rather than on the refresher\'s next tick.
     * Refresh Selector Index
     */
    async catalogRefreshSelectorIndexRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SelectorIndexResponse>> {
        const requestOptions = await this.catalogRefreshSelectorIndexRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SelectorIndexResponseFromJSON(jsonValue));
    }

    /**
     * Re-index the short spellings now, rather than on the refresher\'s next tick.
     * Refresh Selector Index
     */
    async catalogRefreshSelectorIndex(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SelectorIndexResponse> {
        const response = await this.catalogRefreshSelectorIndexRaw(initOverrides);
        return await response.value();
    }

}
