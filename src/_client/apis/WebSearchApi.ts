/* tslint:disable */
/* eslint-disable */
/**
 * otari
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime.js';
import {
    type HTTPValidationError,
    HTTPValidationErrorFromJSON,
    HTTPValidationErrorToJSON,
} from '../models/HTTPValidationError.js';
import {
    type WebSearchBackendResponse,
    WebSearchBackendResponseFromJSON,
    WebSearchBackendResponseToJSON,
} from '../models/WebSearchBackendResponse.js';

export interface WebSearchV1WebSearchSearchGetRequest {
    q: string;
    maxResults?: number | null;
    searchDepth?: string | null;
    topic?: string | null;
    timeRange?: string | null;
    includeAnswer?: boolean | null;
    xGatewayToken?: string | null;
}

/**
 * 
 */
export class WebSearchApi extends runtime.BaseAPI {

    /**
     * Creates request options for webSearchV1WebSearchSearchGet without sending the request
     */
    async webSearchV1WebSearchSearchGetRequestOpts(requestParameters: WebSearchV1WebSearchSearchGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['q'] == null) {
            throw new runtime.RequiredError(
                'q',
                'Required parameter "q" was null or undefined when calling webSearchV1WebSearchSearchGet().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['q'] != null) {
            queryParameters['q'] = requestParameters['q'];
        }

        if (requestParameters['maxResults'] != null) {
            queryParameters['max_results'] = requestParameters['maxResults'];
        }

        if (requestParameters['searchDepth'] != null) {
            queryParameters['search_depth'] = requestParameters['searchDepth'];
        }

        if (requestParameters['topic'] != null) {
            queryParameters['topic'] = requestParameters['topic'];
        }

        if (requestParameters['timeRange'] != null) {
            queryParameters['time_range'] = requestParameters['timeRange'];
        }

        if (requestParameters['includeAnswer'] != null) {
            queryParameters['include_answer'] = requestParameters['includeAnswer'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xGatewayToken'] != null) {
            headerParameters['x-gateway-token'] = String(requestParameters['xGatewayToken']);
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Gateway-Token"] = await this.configuration.apiKey("X-Gateway-Token"); // GatewayTokenAuth authentication
        }


        let urlPath = `/v1/web-search/search`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Run one search through this deployment\'s configured search provider.  The declared query params are the ``provider_options`` keys the providers understand. Everything else the caller sends, ``format`` and ``engines`` included, is ignored rather than forwarded: the bag is opaque to the gateway that filled it, so passing it upstream unread would let a workspace set provider request fields this deployment never chose.
     * Web Search
     */
    async webSearchV1WebSearchSearchGetRaw(requestParameters: WebSearchV1WebSearchSearchGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<WebSearchBackendResponse>> {
        const requestOptions = await this.webSearchV1WebSearchSearchGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => WebSearchBackendResponseFromJSON(jsonValue));
    }

    /**
     * Run one search through this deployment\'s configured search provider.  The declared query params are the ``provider_options`` keys the providers understand. Everything else the caller sends, ``format`` and ``engines`` included, is ignored rather than forwarded: the bag is opaque to the gateway that filled it, so passing it upstream unread would let a workspace set provider request fields this deployment never chose.
     * Web Search
     */
    async webSearchV1WebSearchSearchGet(requestParameters: WebSearchV1WebSearchSearchGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<WebSearchBackendResponse> {
        const response = await this.webSearchV1WebSearchSearchGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
