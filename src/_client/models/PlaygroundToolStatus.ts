/* tslint:disable */
/* eslint-disable */
/**
 * otari
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * Whether one gateway-run tool can be attached right now, and why not.
 * 
 * Three states from two fields, which is what the composer's menu draws: a
 * tool the deployment never configured is not offered, one the deployment
 * configured and this workspace turned off is shown disabled with the reason,
 * and an available one is a plain checkbox. A single boolean would collapse
 * the first two, which is how a checkbox comes to look attachable and then
 * fail at request time (otari-ai#1419).
 * @export
 * @interface PlaygroundToolStatus
 */
export interface PlaygroundToolStatus {
    /**
     * Whether this deployment has a backend for the tool at all.
     * @type {boolean}
     * @memberof PlaygroundToolStatus
     */
    configured: boolean;
    /**
     * Whether the caller's workspace may attach it.
     * @type {boolean}
     * @memberof PlaygroundToolStatus
     */
    enabled: boolean;
    /**
     * Why it cannot be attached. Null when it can.
     * @type {string}
     * @memberof PlaygroundToolStatus
     */
    reason?: string | null;
}

/**
 * Check if a given object implements the PlaygroundToolStatus interface.
 */
export function instanceOfPlaygroundToolStatus(value: object): value is PlaygroundToolStatus {
    if (!('configured' in value) || value['configured'] === undefined) return false;
    if (!('enabled' in value) || value['enabled'] === undefined) return false;
    return true;
}

export function PlaygroundToolStatusFromJSON(json: any): PlaygroundToolStatus {
    return PlaygroundToolStatusFromJSONTyped(json, false);
}

export function PlaygroundToolStatusFromJSONTyped(json: any, ignoreDiscriminator: boolean): PlaygroundToolStatus {
    if (json == null) {
        return json;
    }
    return {
        
        'configured': json['configured'],
        'enabled': json['enabled'],
        'reason': json['reason'] == null ? undefined : json['reason'],
    };
}

export function PlaygroundToolStatusToJSON(json: any): PlaygroundToolStatus {
    return PlaygroundToolStatusToJSONTyped(json, false);
}

export function PlaygroundToolStatusToJSONTyped(value?: PlaygroundToolStatus | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'configured': value['configured'],
        'enabled': value['enabled'],
        'reason': value['reason'],
    };
}

