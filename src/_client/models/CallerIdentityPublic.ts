/* tslint:disable */
/* eslint-disable */
/**
 * otari
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * Who the caller is, as against what they may do.
 * 
 * The account control at the foot of the dashboard's sidebar draws a person,
 * and no authenticated route reported the caller's own name or address, so it
 * drew a role for everybody who could reach it: "Operator", which is not a
 * name and on a multi-tenant deployment was not even true
 * (mozilla-ai/otari#832).
 * 
 * Carried on the membership context for the reason ``deployment_operator`` is:
 * the shell reads that context before it paints, so an identity taken from it
 * needs no request of its own and cannot arrive a beat after the chrome it
 * names. Publishing it costs nothing either, since it is the caller's own
 * identity and they are holding the credential that resolved to it.
 * 
 * ``email`` and ``full_name`` are nullable for opposite reasons. A local
 * operator identity has no address, because first boot provisions it with a
 * name and nothing to sign in with but the master key; a member added by
 * address has no name until they claim the identity and supply one. So a shell
 * has to be ready to draw either one alone.
 * @export
 * @interface CallerIdentityPublic
 */
export interface CallerIdentityPublic {
    /**
     * Whether setting this identity's password claims the deployment, which stops the master key signing in to the dashboard. True for the deployment's operator until it holds a password, whether or not it already has an address; false for everybody else.
     * @type {boolean}
     * @memberof CallerIdentityPublic
     */
    claimsDeployment: boolean;
    /**
     * 
     * @type {string}
     * @memberof CallerIdentityPublic
     */
    email?: string | null;
    /**
     * 
     * @type {string}
     * @memberof CallerIdentityPublic
     */
    fullName?: string | null;
    /**
     * Whether this identity holds a dashboard password. False for one that signs in only through an OAuth provider or a passkey, and for a roster entry nobody has claimed yet. PUT /api/v1/auth/password requires current_password from a cookie-authenticated caller exactly while this is true.
     * @type {boolean}
     * @memberof CallerIdentityPublic
     */
    hasPassword: boolean;
    /**
     * 
     * @type {string}
     * @memberof CallerIdentityPublic
     */
    userId: string;
}

/**
 * Check if a given object implements the CallerIdentityPublic interface.
 */
export function instanceOfCallerIdentityPublic(value: object): value is CallerIdentityPublic {
    if (!('claimsDeployment' in value) || value['claimsDeployment'] === undefined) return false;
    if (!('hasPassword' in value) || value['hasPassword'] === undefined) return false;
    if (!('userId' in value) || value['userId'] === undefined) return false;
    return true;
}

export function CallerIdentityPublicFromJSON(json: any): CallerIdentityPublic {
    return CallerIdentityPublicFromJSONTyped(json, false);
}

export function CallerIdentityPublicFromJSONTyped(json: any, ignoreDiscriminator: boolean): CallerIdentityPublic {
    if (json == null) {
        return json;
    }
    return {
        
        'claimsDeployment': json['claims_deployment'],
        'email': json['email'] == null ? undefined : json['email'],
        'fullName': json['full_name'] == null ? undefined : json['full_name'],
        'hasPassword': json['has_password'],
        'userId': json['user_id'],
    };
}

export function CallerIdentityPublicToJSON(json: any): CallerIdentityPublic {
    return CallerIdentityPublicToJSONTyped(json, false);
}

export function CallerIdentityPublicToJSONTyped(value?: CallerIdentityPublic | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'claims_deployment': value['claimsDeployment'],
        'email': value['email'],
        'full_name': value['fullName'],
        'has_password': value['hasPassword'],
        'user_id': value['userId'],
    };
}

